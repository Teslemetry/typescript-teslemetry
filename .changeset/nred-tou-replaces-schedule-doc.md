---
"@teslemetry/node-red-contrib-teslemetry": patch
---

Document that `setTimeOfUseSettings` replaces the site's entire time-of-use schedule rather than merging with it, in the node's help text, command dropdown, and README.
