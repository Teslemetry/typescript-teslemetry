---
"node-red-contrib-teslemetry": minor
---

Add manual seat cooling (`setSeatCooler`), preconditioning max on/off, climate keeper mode, and bioweapon defense mode on/off command cases to the Command node, wrapping the existing `@teslemetry/api` methods with matching editor dropdown options.
