---
"@teslemetry/node-red-contrib-teslemetry": minor
---

Add a "Get Tariff (TOU rates)" command to the Energy Command node, surfacing the SDK's `getTariff()` read (time-of-use rate schedule) in flows.
