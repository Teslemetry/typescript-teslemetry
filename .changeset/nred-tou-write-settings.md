---
"@teslemetry/node-red-contrib-teslemetry": minor
---

Add `setTimeOfUseSettings` command case to `teslemetry-energy-command`, with strict validation of the `tariffContentV2` tariff document against the SDK's `TariffContentV2` shape and an example flow.
